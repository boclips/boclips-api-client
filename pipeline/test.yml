---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: '10'

inputs:
  - name: source

run:
  path: source/pipeline/test.sh

params:
  CONTRACT_TEST_USERNAME:
  CONTRACT_TEST_PASSWORD:

caches:
  - path: ../../../root/.cache
  - path: ../../../root/.npm
